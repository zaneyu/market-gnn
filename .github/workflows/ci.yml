name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      # The hygiene/stat/PIT tests import only the core deps, so CI runs the full
      # leak harness WITHOUT the heavy torch/GNN stack.
      - run: pip install -e ".[dev]"
      - run: pytest -q
